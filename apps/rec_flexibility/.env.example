# Upstream source schemas
CELINE_SILVER_SCHEMA=ds_dev_silver
CELINE_GOLD_SCHEMA=ds_dev_gold

# Comma-separated device IDs for fleet scope (optional, reads all if unset)
# REC_ACTIVE_DEVICES=device1,device2,...

# Test-phase auto-commit (set to "true" to enable synthetic commitments)
# AUTO_COMMIT_ENABLED=false
